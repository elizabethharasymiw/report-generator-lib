from setuptools import setup, find_packages

setup(
    name='report-generator-lib',
    version='0.1',
    description='Collection of functions to generate reports',
    author='Elizabeth Harasymiw',
    packages=find_packages(),
    install_requires=[],
)
